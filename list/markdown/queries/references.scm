(atx_heading) @heading_reference

(setext_heading) @heading_reference

; (inline_link
;   (link_text) @link_text_reference
; )

; (inline_link
;   (link_destination) @link_destination_reference
; )

; (inline_link
;   (link_title) @link_title_reference
; )

; (full_reference_link
;   (link_text) @link_text_reference
; )

; (full_reference_link
;   (link_label) @link_label_reference
; )

; (collapsed_reference_link
;   (link_text) @link_text_reference
; )

; (collapsed_reference_link
;   (link_label) @link_label_reference
; )

; (shortcut_link
;   (link_text) @link_text_reference
; )

; (image
;   (link_text) @image_alt_reference
; )

; (image
;   (link_destination) @image_destination_reference
; )

; (image
;   (link_title) @image_title_reference
; )

; (autolink) @autolink_reference

; (code_span) @code_span_reference

; (emphasis) @emphasis_reference

; (strong_emphasis) @strong_emphasis_reference

; (strikethrough) @strikethrough_reference

; (footnote_reference
;   (label) @footnote_reference
; )

; (footnote_definition
;   (label) @footnote_definition_reference
; )

(link_reference_definition
  (link_label) @link_definition_reference
)

; (html_tag) @html_tag_reference

(html_block) @html_block_reference

(entity_reference) @entity_reference

; (escaped) @escaped_reference

(backslash_escape) @backslash_escape_reference
